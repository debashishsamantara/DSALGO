//Given a set of coins and a value 'V'. Find the number of ways we can make change of 'V'
//S = {1, 2, 3} V = 3
//Possible ways to make changes are; {3}, {2, 1}, {1, 1, 1}
//Note:- {1, 2} is counted as a separate as it is same as {2, 1}
//To make ways with every coin, we have 2 options
//a) Take it.
//b) Do not take it.
//Since it can be represented as a Recurrence relation, hence it has a Optimal Substructure Property.
//It also Overlapping Subproblem Property

#include <bits/stdc++.h>
using namespace std;

#define vi vector<int>
#define vvi vector<vi>
#define gi greater<int>
#define pii pair<int, int>
#define vii vector<pii>
#define loop(i, a, b) for(int i = a; i < b; i++)
#define ff first
#define ss second
#define setBits(x) builtin_popcount(x)

int main(){
    
    return 0;
}